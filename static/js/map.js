// Generated by CoffeeScript 1.3.3
(function() {

  window.map_image = function(latitude, longitude, container, options) {
    var default_options, image, pieces, url;
    if (options == null) {
      options = {};
    }
    default_options = {
      zoom: 14,
      size: "400x400",
      color: "blue"
    };
    _.defaults(options, default_options);
    pieces = ["center=" + latitude + "," + longitude, "zoom=" + options.zoom, "size=" + options.size, "sensor=true", "markers=color:" + options.color + "%7Clabel:S%7C" + latitude + "," + longitude];
    url = "http://maps.googleapis.com/maps/api/staticmap?" + pieces.join("&");
    image = jQuery("<img src=" + url + " width=\"400\" height=\"400\">");
    return jQuery("body").append(image);
  };

}).call(this);
